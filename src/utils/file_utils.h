#ifndef FILE_UTILS_H
#define FILE_UTILS_H

#include <string>
#include <vector>

// Function to load a file into memory
std::vector<char> load_file_to_memory(const std::string &file_path);

#endif // FILE_UTILS_H
